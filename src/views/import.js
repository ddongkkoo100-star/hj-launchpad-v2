// Tasks Import view — Step 1 placeholder
// Step 4에서 본격 구현: [HJ_EXECUTION_GROUP] 마커 파서.

export function renderImport(root) {
  root.innerHTML = `
    <div class="section-title">Tasks Import</div>
    <div class="placeholder">
      <strong>Tasks Import (Step 4 예정)</strong>
      Grok Tasks 결과를 일괄 붙여넣어 카드 슬롯에 자동 분배.
      <span class="hint">[HJ_EXECUTION_GROUP], [HJ_OUTPUT_SLOTS] 마커 기반 파서</span>
      <span class="hint">대상 그룹: COLLECT-1, COLLECT-2, COLLECT-3, NOMAD-X, DT-COLLECT</span>
    </div>
  `;
}
