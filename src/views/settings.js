// 설정 view — Step 1 placeholder

export function renderSettings(root) {
  root.innerHTML = `
    <div class="section-title">설정</div>
    <div class="placeholder">
      <strong>설정 (추후 단계)</strong>
      테마 / 보유 종목 / Tasks 그룹 토글 등.
      <span class="hint">HJ 런치패드 v0.1 — Step 1 골격</span>
    </div>
  `;
}
